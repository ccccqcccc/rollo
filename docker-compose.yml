version: "3.9"
services:
  bitcoin:
    container_name: bitcoin
    image: ruimarinho/bitcoin-core:latest
    command: -printtoconsole -blocksonly -prune=550 -maxmempool=5 -maxconnections=5
    restart: always
    stdin_open: true
    tty: true
    volumes:
      - ./bitcoin.conf:/root/.bitcoin/bitcoin.conf
      - ./bitcoin.conf:/home/bitcoin/.bitcoin/bitcoin.conf
      - bitcoin:/home/bitcoin/.bitcoin
    ports:
      - 8332:8332
    networks:
      - rollo
  # ethereum:
  #   container_name: ethereum
  #   image: ethereum/client-go:stable
  #   command: --syncmode light --http
  #   restart: always
  #   stdin_open: true
  #   tty: true
  #   volumes:
  #     - ethereum:/root/.ethereum
  #   ports:
  #     - 8545:8545
networks:
  rollo:
    driver: bridge
volumes:
  bitcoin:
  # ethereum:
